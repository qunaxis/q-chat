import { Router } from 'express'

const routes = Router()



routes.get('/', (req, res, next) => {
  // let socketId = []
  // const io = req.app.get('socketio')
  

  // res.json({ message: `All right, it's /chat GET route` })
})



export default routes
